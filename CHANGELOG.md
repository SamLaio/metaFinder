# 變更紀錄

## 0.1.15 - 2026-09-25

- ISBN／eISBN 查詢在站內搜尋找到第一個商品頁後，立即進入 ISBN 精確驗證，不再為蒐集其他站台耗盡總搜尋時間，避免已知 ISBN 的書籍被 CLI 逾時後誤判為無候選；加入回歸測試。

## 0.1.14 - 2026-09-25

- KADOKAWA 官方商品頁現在辨識為出版社來源，擷取作者、出版日、ISBN、完整簡介與封面；日文原文書同步標示 `原文`，避免整理流程誤判為缺譯者。
- 查詢字串明示 `カドカワBOOKS` 或 `KADOKAWA` 時，改用 KADOKAWA 官方 JSON 搜尋，直接取得商品頁，不再依賴可能回傳無關結果的公開搜尋。
- 晉江文學城等網路連載頁不再把站台誤當出版社；無出版社資料時統一標示為 `網版`，站台網址仍保留為候選證據。

## 0.1.13 - 2026-09-25

- 強化誠品商品頁的候選辨識與排序，避免《高年級實習生》等一般中文書在有效商品頁已出現時仍回傳無候選；加入回歸測試。

## 0.1.12 - 2026-09-23

- 含「書評／宣傳引言」的尾端括號書名，會先產生移除宣傳語、保留作者的核心查詢變體；例如《龍與地下鐵（「文字鬼才」馬伯庸大開腦洞之作）》可正確查找《龍與地下鐵》。新增回歸測試。

- 補上岡田和人的已核對國籍標籤「日本」，使中文姓名的日本漫畫家也能在回填時取得作者地區標籤。

- 三民站內搜尋可辨識「書名 02 作者」的尾端集數，先過濾正確冊次再套用結果上限，避免《迷宮飯》一類系列被較新集數搶走候選；新增回歸測試。

- 書名加作者或集數查找會先解析原始查詢得到的首批可信書店商品頁，避免後續多變體搜尋耗盡時間預算而遺失可用候選資料；新增回歸測試。

- ISBN／eISBN 查找若已從書店站內搜尋取得精準候選，會立即回傳，不再繼續執行可能耗盡整體時間預算的多站蒐集與頁面解析；避免《迷宮飯》等已有 ISBN 資料的書籍錯誤回傳無候選。新增回歸測試。

- `metafinder search` 新增真正的整體查找硬性時間上限：個別站台請求或 HTML 解析卡住超過 `--max-search-seconds` 時，CLI 會回傳空候選、exit code `1`，並於 stderr 標示查找逾時，不再使 Calibre 批次無限等待。加入回歸測試。

- 補入已核對的陳鴻圖＝臺灣作者地區對照，讓其書目可安全補上臺灣標籤。
- 補入已核對的藤井太洋＝日本作者地區對照，讓其作品可安全補上日本標籤。

- 修正書名尾端集數在作者之前時未被辨識的問題，例如 `農林14 白鳥士郎` 與 `帶著外掛轉生為公會櫃台小姐2.5 夏にコタツ`；不同冊候選現在會在相關性檢查階段排除，避免第 6 集或第 12 集的 ISBN、譯者與簡介誤寫入其他集數。

- 補入已核對的蔡小雀＝臺灣作者地區對照，舊版言情作品整理時可安全補上臺灣標籤。
- 查詢書名若以 `～系列之第幾冊` 結尾，會優先以移除該系列後綴、保留作者的核心書名查找，並在候選篩選與排序實際使用該變體，避免舊式言情系列因書名格式與短預算而漏查。
- 修正書名查找流程：站內搜尋回傳 URL 後仍保留 Web 後備搜尋，避免三民或起點的雜訊結果使博客來、Kobo 等正確商品頁完全不會被嘗試；起點手機站搜尋改在書店來源全數無結果後才使用，降低一般中文書查找的無效請求。
- 商品頁遭臨時封鎖時，若公開搜尋結果的書店頁標題仍與查詢精確相符，CLI 會保留低信任的 `web-search-result` 候選連結供核對；它不具可自動寫回書庫的資料證據。
- 修正書名相關性排序把查詢詞誤當成完整候選書名比較的問題；現在可正確比對「高年級實習生」與「高年級實習生：馬里亞納海溝跳島記」這類帶副標商品頁。
- 作者地區辨識會先移除「原作：」等信用角色前綴，並補入已核對的貞本義行＝日本對照，讓書目流程能安全寫入日本標籤。
- 簡介擷取改用保留段落的文字清理，Readmoo、三民與 WooCommerce 內容區塊不再把 `<p>`／`<br>` 斷行壓成空格；新增回歸測試。
- Readmoo 商品頁改為優先擷取實際的書籍簡介區塊，避免只寫入遭省略號截斷的 HTML／Open Graph 摘要；新增回歸測試。
- 系列辨識新增「作品名 卷一／卷 4（終）」格式，系列名稱會去除「卷」字並保留正確集數；新增回歸測試。
- 收斂軍事標籤判斷為「軍事小說／軍事題材」等明確語境，避免「軍事狂兔」等情節文字誤加軍事分類；新增回歸測試。
- 修正三民網路書店的系列集數搜尋：保留原始前置集數查詢並先在完整結果中篩選目標集數，再套用筆數上限；未指定漫畫時優先原作小說，避免網站以最新集數排序而遺漏早期集數。

## 未發佈

- 書店搜尋摘要即使已包含 ISBN 與封面，也會再解析一次商品詳頁；修正博客來 ISBN 查詢在完整度門檻誤跳過詳頁，造成譯者、出版社、出版日、簡介與標籤缺漏的問題。新增回歸測試。

- ISBN 查找遇到只有搜尋卡片證據的精準候選時，不再提早結束；商品詳頁遭封鎖或資料不足時，會繼續嘗試其他可信書店的詳頁，以取得可核對的完整書目。新增回歸測試。

- 新增作者國籍／地區標籤判斷：採用已核對作者對照表，並只對日文假名與韓文字等無歧義文字系統自動加入地區標籤；不以中文或拉丁姓名猜測國籍。Calibre 書庫流程改由此共用規則補標。

- 候選相關性判斷不再把商店副標中「《某書》作者」等括號內引用書名當成主書名，避免同作者的另一部作品誤配；新增回歸測試。
- 書名加作者查詢會額外嘗試移除尾端英文作者別名的變體，例如 `娜塔夏‧ 布朗（Natasha Brown）`，避免 Calibre 匯入欄位的雙語作者格式導致可靠候選被排除。
- 候選作者比對也會移除尾端英文別名，並統一 `·`、`・`、`‧` 與 `．` 的間隔號形式，確保搜尋變體不會在最終相關性篩選被再次排除。

- 候選排序會優先採用具實質內容的書籍簡介，降低只有短促宣傳語或截斷預覽的商店頁排在完整商品簡介之前的機率。
- 略過 `xxxx-xxxx` 等匯入占位識別碼查詢，避免把無效 ISBN 送至書店而誤配無關商品；呼叫端可安全改用書名與作者重試。
- 集數相關性比對支援緊接書名的前置零數字，例如 `怪島奇譚02` 與 `怪島奇譚 2`；完整書名頁不再因集數空白格式不同而輸給短預覽候選。

- 查詢尾端已帶作者時，商店與網路小說候選都必須通過作者比對；漫畫候選只把該作者列為原作時亦不視為同一版本，避免同名漫畫版或部分書名相似的其他作品誤配。
- 明確集數查詢不再接受缺少集數的候選，並辨識「全集8」等黏接冊數，避免同名不同版誤配。
- 單一書名查詢排除額外帶有 `＆` 或 `&` 的合訂版候選。

- 可見書目欄位要求標籤後有冒號、空白或換行邊界，不再把目錄的「譯者序」誤認為名叫「序」的譯者；無明確譯者欄位時留空供核對。

- 搜尋 API 與共用文字清理移除匯入 PDF 書目殘留的 NUL 控制字元；
  Calibre 呼叫端也在建立命令列參數前清理，避免工具尚未啟動即因 embedded null character 失敗。

- 將非 ISBN 商品條碼的排除移至共用 `normalize_isbn`，同時涵蓋 ISBN、eISBN、
  JSON-LD 與可見標籤，修正三民補丁刪除 471 條碼後又被後續解析補回的問題。
  新增實際條碼與解析順序回歸測試；保留合法格式的 ISBN10／978／979 候選。

- 支援 WooCommerce 書店／出版社的獨立內容簡介頁籤，優先讀取 `tab-description`，
  不混入作者簡介、序文及商品規格；補足聯經官網有簡介但結果為空的情況，新增回歸測試。

- 單冊集數查詢排除套書、合輯與「全 N 冊」候選，避免第四冊誤配全四冊；保留套書本身的查詢。補上回歸測試。
- 聯經出版事業股份有限公司、聯經出版社與聯經統一為聯經出版，並補別名測試。

- 三民分割標籤中的譯者改從書目列完整讀取，避免被後方「譯者簡介」覆蓋成「簡介」，並保留多位譯者。

- 三民商品頁優先擷取商品簡介區塊，避免摘要中的規格表遮蔽真正簡介；排除非 978/979 開頭的 13 位店內商品碼，不再誤存為 ISBN。

- 修正書名尾端多位數集數被拆成最後一位的問題；第 16 集不再被誤判為第 6 集。

- 新增三民站內商品搜尋並過濾不相干的推薦品，讓缺 ISBN 的系列書能直接找到可解析的商品頁。

- 起點手機搜尋先比對結果卡片書名，排除熱門推薦與無標題連結，避免無關作品佔滿候選名額。
- 搜尋時間預算保留最多三分之一給商品頁解析，降低收集完網址卻來不及讀取 metadata 的空結果。

- 晉江文案在正文簡介之後遇到預收、新坑或下一本宣傳時停止擷取，避免混入其他作品的故事；開頭宣傳仍跳過後繼續讀取本書簡介。

- 起點中文網作品頁若搜尋結果命中 `www.qidian.com/book/<id>/`，會自動改抓
  `m.qidian.com/book/<id>/` 手機版頁面，避開桌面版防爬空殼，讓 JSON-LD
  metadata 可正常解析。
- 新增起點手機站內搜尋，從 `m.qidian.com/search` 搜尋結果抽出作品 `bid`，
  讓一般 `書名 作者` 查詢也能找到起點作品頁。
- 公開搜尋優先來源加入東立官網 `site:tongli.com.tw`，改善東立輕小說在無
  ISBN、僅以書名作者查詢時漏掉出版社、譯者與出版資訊的情況。
- 人名欄位清洗會將作者、譯者、繪者中的 `·`、`・`、`‧` 正規化為臺灣書目慣用的 `．`，
  並移除間隔號兩側多餘空白，例如 `戴維·王` 會輸出為 `戴維．王`。
- 地區標籤推論新增 `葡萄牙`，讓葡萄牙作者或來源標籤可正確保留為 Calibre
  作者地區標籤。
- 出版社別名新增 `城邦出版集團 麥田` -> `麥田出版`、`英屬維京群島商高寶國際有限公司台灣分公司`
  與 `高寶書版` -> `高寶`，降低同一出版社在 Calibre DB 內分裂。
- 網文來源候選若遇到「書名 作者」查詢且候選作者完全不符，會排除該候選，
  避免短書名如 `異形` 被起點等同名網文誤判為翻譯書 metadata。

## 0.1.11 - 2026-09-03

- 新增通用簡介清洗規則：當網頁 `description` 把 `Publisher/Author` 或 `出版/作者`
  欄位前綴和真正簡介串在一起時，會剝除欄位前綴，只保留可寫入 Calibre comments 的簡介內容。
- 補強晉江文學城作品頁解析：可從 `文案` 區塊抽取簡介，並讀取 `內容標籤` 與
  `文章類型` 作為標籤候選，避免網文整理時只抓到書名、作者與出版社。
- 收斂晉江文學城標籤擷取範圍：遇到 `搜索關鍵字`、主角、配角、其它等作品檢索欄位
  時即停止讀取內容標籤，避免把角色名或作者備註寫成 Calibre 標籤。
- 晉江文學城內容標籤現在只收明確題材詞；若頁面把角色名或搜尋提示混在 `內容標籤`
  區塊下，會跳過這些非分類文字。

## 0.1.10 - 2026-08-30

- 修正書店頁面將「書名、語言、ISBN、頁數、出版社、作者、出版日期」等商品規格誤當成
  `description` 的問題；這類資料現在會被視為 metadata fact sheet 並清空，避免寫入 Calibre
  comments 後變成錯誤簡介。
- 新增 Pubu 商品頁專用解析：可清除 `出版/Publisher` 與 `作者/Author` 前綴，只保留真正簡介，
  並從頁面資訊欄抽取發行日與語言。
- HTTP 抓頁時加入 `Accept-Language: zh-TW`，降低 Pubu 等站台回傳英文前綴 metadata 的機率。
- JSON-LD 解析會讀取 `inLanguage`，並對含控制字元的 JSON-LD 做保守寬鬆解析，避免 Pubu 頁面
  因少量非法字元而漏掉書名、作者、出版社或封面。
- JSON-LD 作者欄若以英文逗號串接中日韓作者名，會保守拆成多位作者，避免 Pubu 頁面把
  `青山美智子,田中達也` 寫成單一作者。
- ISBN 站內搜尋不再因博客來先命中就停止查找其他來源，且在已有候選時仍會用剩餘時間執行公開
  搜尋補來源；當博客來只有規格表或缺少有效簡介時，Pubu、Readmoo、誠品等後續來源仍有機會
  進入候選，最後再以 ISBN 精確過濾。
- 對 `books.com.tw` 的直接商品頁與站內搜尋請求加入至少 3 秒間隔，降低博客來防爬蟲造成查詢失敗
  或回傳不完整內容的機率。

## 0.1.9 - 2026-08-29

- 博客來等書店站內搜尋若只取得書名與作者的摘要卡片，現在會在時間預算內補抓第一筆商品頁，
  取得 ISBN、出版社、出版日期與標籤等完整 metadata，避免摘要候選被誤當成已完成資料。
- 低搜尋預算下，公開搜尋現在會先嘗試書店來源再嘗試網文提示，並加入三民、城邦讀書花園與金石堂，
  改善一般出版品在批次整理時只得到空候選、因而漏掉標籤的情況。
- 搜尋 query 變體新增臺灣正體化版本：例如匯入書名含 `什麽` 時，會額外嘗試
  `什麼`，改善博客來、青文等繁中來源的命中率。
- 搜尋 query 與候選比對新增常見臺灣字形變體：例如 `櫃檯` 會額外嘗試
  `櫃臺`、`櫃台`，避免《帶著外掛轉生為公會櫃檯小姐》這類書名因來源用字不同而漏收。
- 新增三民網路書店商品頁解析，可從直接網址抽取 ISBN13、出版社、作者、譯者、出版日與簡介，
  改善人工找到三民頁面後仍無法讓 `metaFinder` 寫回 metadata 的問題。
- 新增 `tdtb.org` 館藏頁解析，可抽取作者、出版單位、出版年份與簡介，支援舊書館藏頁作為補缺來源。
- 修正直接輸入商品 URL 時，網址中的商品編號被誤當成 expected ISBN 而濾掉正確候選的問題。
- 預設公開搜尋來源加入 `site:tdtb.org/library`，讓舊書館藏頁有機會在批次 metadata 補缺時被自動收進候選。
- 站內搜尋新增 `tdtb.org/library` 表單查詢，會把 `書名 作者` 拆成館藏站的 `Title` / `Author`
  參數，並嘗試移除前置集數後查找舊書館藏頁。
- URL 收集階段若站內來源已找到候選，會先回傳候選讓 parser 解析，不再繼續消耗時間跑公開搜尋，
  避免批次整理在短時間預算下明明找到來源卻逾時回空。
- 館藏頁可見欄位解析會辨識空欄位，避免把下一個欄位名稱如 `出版年份`、`格式類型`
  誤當成出版社或出版日期。

## 0.1.8 - 2026-08-21

- 修正明確集數查詢的相關性判斷：除了 `01 書名` 這類前置集數，也會辨識
  `書名 11 作者` 這類放在作者前的集數，並排除候選書名或系列集數不一致的結果，
  避免查第 11 集或第 20 集時誤採同系列第 8 集。
- 新增出版社名稱正規化：依 Calibre DB 既有慣例，將 `聯經出版公司` 正規化為
  `聯經出版`，並將 `東立出版社`、`東立` 正規化為 `東立出版`，避免批次整理時
  同一出版社被寫成多種名稱。

## 0.1.7 - 2026-08-16

- 補強雙語書名搜尋變體：當匯入書名是英文標題在前、中文標題在後時，會額外嘗試中文標題在前、英文標題在後，以及括號集數如 `(01)` 的查詢形式，改善系列書 metadata 查找命中率。

## 0.1.6 - 2026-08-15

- ISBN 公開搜尋 query 新增博客來電子書產品路徑提示 `site:books.com.tw/products/E`，讓低搜尋
  預算下更容易命中博客來 `E` 開頭電子書商品頁，例如《LV999的村民》第 1、8 集電子書頁。
- ISBN 站內搜尋在高優先來源已命中時會先停止收集後續來源，保留時間解析已找到的商品頁，
  避免低搜尋預算下明明找到博客來電子書頁，卻因繼續掃其他來源而逾時回空。
- 博客來 ISBN 搜尋結果若同時列出紙本與電子書，現在會保留多個商品候選；一般書名搜尋仍維持
  多筆結果不猜測，避免誤收。

## 0.1.5 - 2026-08-15

- 修正地區標籤推論：不再從出版社名稱推導作者/作品地區，避免臺灣通路或臺灣代理出版社
  讓日本、韓國等翻譯作品被誤標為 `臺灣`。
- 降低 `metaFinder` 預設搜尋時間預算：單一 HTTP 請求預設 3 秒、整體搜尋預設 12 秒、
  公開搜尋 query 預設 4 組，避免 Calibre 批次整理時外部搜尋引擎或書店頁面慢回應，
  造成每本都被外層 20 秒逾時直接砍掉。
- 修正公開搜尋 fallback：DuckDuckGo 暫時逾時或失敗時，現在會繼續嘗試 Bing，避免 ISBN 搜尋
  因單一搜尋引擎連線失敗而回傳空候選。
- ISBN 優先搜尋來源加入 `site:crown.com.tw` 與 `site:eslite.com`，並新增皇冠文化
  `view.aspx?bc=` 商品頁 URL 規則，改善皇冠出版舊書或紙書 metadata 的命中率。
- 搜尋 query 變體新增「去除書名前置集數」備援：例如 `01 86-不存在的戰區 安里アサト`
  會保留原查詢，並額外嘗試 `86-不存在的戰區 安里アサト`，避免 Calibre 系列書名把集數放
  在書名前面時找不到 metadata。
- 前置集數會同時展開有限模糊變體，例如 `書名 1 作者`、`書名 第1集 作者`、
  `書名 第一集 作者`、`書名 vol.1 作者`、`書名（1） 作者`，涵蓋來源常見集數標示差異。
- 前置集數規則只處理明確格式，例如 `01 書名`、`第2集 書名`、`（03）書名`，避免把
  `5.18光州！光州！` 這類日期或事件型書名誤切。
- 搜尋候選若查詢含前置集數，會比對候選標題或系列集數，排除明顯不同集數的結果，例如查第
  1 集時不再接受同系列第 9 集。
- 站內搜尋 URL 收集與集數模糊變體都新增上限，避免整體時間預算耗在大量來源查詢，導致已找到
  的來源頁尚未解析就逾時，或完全沒結果時長時間等待。
- 相關性比對新增長核心詞保護：若查詢沒有作者命中，且書名中的 4 字以上核心詞完全沒命中，
  不再只因共通數字或泛詞相同就接受候選，例如 `21世紀的21位思想家` 不會誤收
  `21世紀的21堂課`。
- 新增 Anobii 書頁 URL 規則與輔助解析：可從 `/books/<slug>/<isbn>/<id>` URL 抽出 ISBN，
  並避免把 Anobii 的通用登入/歡迎頁標題誤當書名。
- 修正博客來搜尋結果頁 URL 正規化：可將
  `search.books.com.tw/redirect/move/.../item/<產品編號>/...` 轉成
  `www.books.com.tw/products/<產品編號>`，讓 ISBN 搜尋能解析博客來產品頁。
- 新增城邦讀書花園 `cite.com.tw/book?id=` 書頁來源規則，供人工搜尋或公開搜尋命中時直接解析。
- ISBN 查詢新增 Open Library 輔助候選，改善外文 ISBN 在主要華文書店查不到時完全無候選的情況。
- 新增 QQ 閱讀 `ubook.reader.qq.com/book-detail/` 與起點中文網 `qidian.com/book/` 書頁規則，讓人工搜尋或可信入口取得網址時可由 `metaFinder` 解析/辨識為網文來源。
- 非 URL、非 ISBN 搜尋時，若公開搜尋命中未知網域且候選只有頁面標題、沒有作者/出版社/ISBN/封面等 metadata 證據，會被排除；並從自動候選排除巴哈動畫頁與 Wikipedia，避免 `86` 這類書名誤收動畫播放頁或百科條目。
- 前置集數查詢的站內搜尋改為優先嘗試去前綴後的顯式集數變體，且每個變體只收少量 URL，避免原始 `01 書名` 搜尋結果塞滿上限，導致 `書名 1 作者` 這類真正能命中正確集數的變體沒有機會執行。
- 使用 ISBN 查博客來且搜尋頁只有唯一結果時，搜尋摘要候選會保留查詢 ISBN，避免產品頁暫時回空殼時，唯一結果又被 ISBN 精準過濾誤刪。

## 2026-08-08

- 縮窄 `BL` 標籤推論規則：不再因 Pubu、Readmoo 等頁面導覽或活動文字出現裸字
  `BL` 就把一般書籍誤標為 `BL`；只有 `BL小說`、`BL漫畫`、`耽美`、
  `boy's love` 等明確類型文字才會觸發，並補上回歸測試。
- 搜尋提示新增 `番茄小說` / `番茄小说` / `fanqienovel`，讓番茄網文作品更容易先找到官方作品頁，
  避免只命中鏡像站或轉載站。

## 2026-08-07

- 相關性比對新增排除「同作者 + 相同集數數字」但書名核心詞不同的候選；例如查詢
  `滅亡後的世界06 sing N song` 時，不會再把同作者的
  `全知讀者視角06 - sing N song` 視為有效候選。

## 2026-07-25

- 簡轉正改為優先使用 `D:\github\zhTranslate` 共用文字轉換層；`metaFinder` 不再直接讀取自己的 `custom_replacements.tsv` 作為主要詞庫。
- 簡轉正載入順序改為先插入本機 `D:\github\zhTranslate\src` 再匯入 `s2tw_converter`，避免吃到舊安裝版而漏掉最新共用詞表。
- 晉江文學城作品頁新增站點補丁：直接解析 `onebook.php?novelid=` / `book2/<id>` URL 時，會把 `《書名》作者_晉江文學城` 這類 SEO title 清為真正書名與作者，補上 publisher，並抽取頁面封面 URL。
- ISBN 查詢若沒有任何候選的 `isbn` 或 `eisbn` 精準命中查詢值，現在會回傳空候選，避免 Pubu 等站內搜尋回傳無關熱門書時被誤採用。
- `metafinder-import-replacements` 預設輸出改為 `D:\github\zhTranslate\src\s2tw_converter\custom_replacements.tsv`，避免新增詞彙寫到不再生效的舊位置。
- 修正搜尋流程容易被外部搜尋卡住的問題：`MetadataFinder` 新增單一請求逾時、整體搜尋時間預算與 web query 數量上限，CLI 對應新增 `--request-timeout`、`--max-search-seconds`、`--max-web-queries`。
- ISBN 查詢不再展開成大量書名/作者與網路小說提示查詢；改以 ISBN 本身搭配主要書店、出版社、Google Books 與圖書館來源搜尋，避免無關來源讓批次整理等待時間倍增。
- 相關性比對新增正體 / 簡體查詢變體：當使用正體中文查詢時，可接受晉江等官方頁回傳的簡體書名與作者候選，避免 `女神的煩惱 林綿綿` 這類查詢把 `女神的烦恼 / 林绵绵` 官方頁誤排除。
- 站點來源規則抽成 `source_rules.py`，把來源名稱、類型、URL pattern 與 site patch 集中管理，讓 `site_search` 與 parser 共用同一份規則。
- `GenericPageParser` 拆成共通抽取、站點補丁與 fallback 抽取三段流程，番茄小說這類特殊頁面改以小型 patch hook 處理，降低主 parser 的條件分支膨脹。
- README 新增參考來源小節，明確標示 `chihchun/library-helper` 作為站點規則與頁面抽取分層的借鑑來源。

## 2026-07-24

- 改善書名加作者查詢的相關性判斷：候選若只命中作者、沒有命中任何書名核心詞，會被排除，避免同作者不同作品在批次整理時被誤當成正確 metadata。
- 搜尋階段會替正體中文查詢補上簡體中文變體，改善晉江等以簡體標題建立索引的官方作品頁命中率；例如 `社畜也能成為魔法少女嗎 盧貝多` 會額外嘗試 `社畜也能成为魔法少女吗 卢贝多`。
- 新增系列解析欄位與規則：`BookMetadata` 現在會輸出 `series` / `series_index`，可從 `～系列之三`、`PART9`、`NO.4`、`Part 2` 與上下冊格式解析候選系列資料。
- 新增系列證據優先序：明確書名系列標記優先於外部書目表，外部書目表優先於單純上下冊/尾端數字；並將 `溫馨` 這類疑似分類字樣降權，避免誤覆蓋真正系列。
- 修正同作者不同書的誤收問題：候選標題若是 `書名 - 作者` 格式，相關性比對會先移除尾端作者，避免查詢 `盲眼刺客 瑪格麗特．愛特伍` 時把 `使女的故事 - 瑪格麗特．愛特伍` 誤判成標題命中。

## 2026-07-17

- 新增番茄小說作品頁解析：辨識 `fanqienovel.com/page/<id>`，從頁面可見欄位抽取書名、作者、類型標籤、最新更新時間與簡介，並標為 `web-novel` 來源。

## 2026-07-16

- 改善書名查詢候選排序：當查詢同時含有完整書名與作者時，完整命中書名與作者的候選會優先於只命中部分泛詞的候選，避免例如「新時代，新魔法」被「新時代女性」類同詞書名排在前面。
- 新增回歸測試，覆蓋完整書名與作者命中應優先於高分鬆散詞命中的情境。
- 書名比對會辨識晉江等網路小說頁常見的 `《書名》作者_站名` 標題格式，並將 `jjwxc.net` / `m.jjwxc.net` 納入預設站點搜尋。
- 補上晉江作品頁 URL 規則與「晉江文學城 / 晋江文学城 / jjwxc」查詢提示，讓網路小說 metadata 搜尋更容易收進正確官方作品頁。
- 多詞查詢只命中單一泛詞時不再視為有效候選，避免找不到正確官方頁時把同名詞但不同書、不同作者的書誤報為候選。
- 搜尋候選 JSON 會在候選頂層同步輸出 `cover_url`，並補強封面抽取，支援 `og:image:secure_url`、`twitter:image:src`、JSON-LD `ImageObject.contentUrl` 與常見 lazy-load 圖片屬性。

## 2026-07-13

- 補強 agent 慣例：Calibre 書庫整理流程中，只要 metadata 判斷可泛用，就不應只留在一次性手工判斷，應沉澱到本專案。
- 明確列出適合沉澱的規則類型，包括來源優先序、ISBN/eISBN、作者與譯者清理、出版日、系列、標籤、得獎標籤、不推論來源欄位、站台 parser、封面 URL 與簡轉正詞彙。
- 明確規定沉澱規則時要修改實作或資料檔、補 regression test、更新 README 或使用說明、更新 change log，並執行 `pytest -q`。
- 強化簡轉正自訂替換：OpenCC 轉換前後都會套用 `custom_replacements.tsv`，讓簡體 key 與正體 key 都能命中，例如 `实时` 可正確轉為 `即時`。
